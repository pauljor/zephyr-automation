---
description: Process exactly ONE unprocessed row from the Zephyr/JIRA sync task, then stop
---

Follow the workflow defined in `.claude/tasks/zephyr-jira-sync.md` in this repo, but process **only a single row** on this invocation, then stop. Do not loop over multiple rows.

Steps:

1. Read `.claude/tasks/zephyr-jira-sync.md` for the full spec (env vars, column map, JIRA/Zephyr logic).
2. Load the Excel workbook at `TEST_DATA_URL` (sheet `TEST_DATA_SHEET`) — it's binary, so parse it with a Python/`openpyxl` script via Bash rather than `Read`ing it as text (see "Loading `TEST_DATA_URL`" in the spec). Filter to rows where column D (the assignee column, header exports blank but the values are real names) equals `Paul` (case-insensitive exact match).
3. Read `CSV_LOGS` and collect the set of column-B (`Execution.Key`) values already logged, regardless of status (`PASSED`/`FAILED`/`SKIPPED`/`BLOCKED`/`NOT EXECUTED`).
4. Pick the **first** matching row from step 2 whose column B is NOT yet in the log from step 3. This is the one row to process.
   - If no unprocessed matching row remains, report that and stop — do not process anything.
5. Process that single row exactly per the spec:
   - Check column G against the JIRA board (`JIRA_URL`) for an existing ticket. This task is read-only against JIRA — no comment or transition is made on the JIRA issue itself.
   - If found: record it on the **Zephyr execution** instead, via `PUT https://prod-api.zephyr4jiracloud.com/v2/testexecutions/<column B>` with body `{"comment": "<p>JIRA <code>&lt;ticket key&gt;</code> Already Automated</p>"}` (no `statusName` — no test was run). Re-`GET` to confirm the `comment` field actually changed (this comment-only shape, with no `statusName`, is unconfirmed until exercised live — if it doesn't take, or the API rejects the body without `statusName`, treat as blocked rather than guessing a workaround). Then append `<column B>, SKIPPED, JIRA:<ticket key>` to `CSV_LOGS`.
   - If not found: determine whether an automated test in `teacher-student-automation` already covers this scenario. **Before writing a new test, also check `CSV_LOGS` for a prior row with the same column F (Test Case.Key) value** — the same test case can recur across multiple execution rows/cycles, and a test may already exist from an earlier processed row; reuse it instead of duplicating. Only write a new test if genuinely none exists either way (per that repo's conventions), run it for real against the live QA host, then record the actual Passed/Failed result **and** a structured record in one call: `PUT https://prod-api.zephyr4jiracloud.com/v2/testexecutions/<column B>` with `{"statusName": "Pass"|"Fail", "comment": "<STEPS/EXPECTED/ACTUAL as HTML>"}` — the `comment` field is raw HTML, not markdown (Zephyr's own comment editor renders `<p>`/`<b>`/`<ol>`/`<ul>`/`<li>`/`<code>` tags; a plain string with `**bold**`/backticks/`\n` gets inserted as one flattened, literal text run instead). Three sections, in that order: `<p><b>STEPS</b></p>` + `<ol><li>...</li></ol>` (numbered), `<p><b>EXPECTED</b></p>` + `<ul><li>...</li></ul>` (bulleted), `<p><b>ACTUAL</b></p>` + `<ul><li>...</li></ul>` with `<b>PASSED</b>`/`<b>FAILED</b>` — no `::`, curly braces, or em dash anywhere in it, those have triggered a `400 "Invalid Payload"` before. Write all three sections in plain, everyday language, as if explaining the test to a non-technical reader (a PM, a teacher, a ticket reviewer) rather than another engineer — no status codes, endpoint paths, pytest output, or file names in the comment text; use `<code>` only around a value the reader genuinely needs verbatim (e.g. the malformed input tried), not around internal identifiers. STEPS comes from what the test actually does, ACTUAL from what really happened on this run — described in plain language, not technical detail. See "Recording the automation" in the spec for the exact template. This is the only mechanism available: Zephyr Essential has no comment endpoint for test cases and no attachment endpoint at all, only this execution-level `comment` field. See "Zephyr Essential API" in the spec for the exact request shape and why no MCP tool can do this step. Re-`GET` to confirm `testExecutionStatus.id` actually changed (`14539939`=Pass, `14539940`=Fail); if it didn't, treat as blocked rather than guessing a result. Then append `<column B>, PASSED, JIRA:NONE` or `<column B>, FAILED, JIRA:NONE` to `CSV_LOGS` matching the confirmed result — a `FAILED` row is a real bug find, not an error, so log it and move on.
   - If the row can't be resolved (ambiguous JIRA match, an API error, or an unconfirmed Zephyr write): append `<column B>, BLOCKED, REASON:<short description>` to `CSV_LOGS` and report the blocker instead of guessing.
6. **No confirmation needed before mutating.** Proceed straight through Zephyr mutations (comment, status update) and the `CSV_LOGS` write without pausing to ask — just report which row (column B, F, G values) and action was taken, after the fact, in the final summary. JIRA itself is never mutated (read-only JQL search/`getJiraIssue` only).
7. After processing the one row, stop and report the result. Do not automatically continue to the next row — I will run `/zephyr-sync-one` again for the next item.
